machine:
  timezone:
    America/Los_Angeles
  ruby:
    version: 2.2.0

deployment:
  production:
    branch: master
    commands:
      - git push web@luvtap.us:repos/luvtap.git $CIRCLE_SHA1:master
